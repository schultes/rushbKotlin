<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.thm.sbwl47.rush_b_vanilla">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Rush B"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RushBVanilla">
        <activity android:name=".view.activity.GameActivity"></activity>
        <activity android:name=".view.activity.HelpContentActivity"></activity>
        <activity android:name=".view.activity.LevelMenuActivity"></activity>
        <activity android:name=".view.activity.SettingsMenuActivity"></activity>
        <activity android:name=".view.activity.GameMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>